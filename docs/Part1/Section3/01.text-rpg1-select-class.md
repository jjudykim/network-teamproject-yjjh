## 🔹Text **RPG 프로젝트의 “직업 선택” 기능 구현 목표**

---

- 직업 선택 메뉴 구현
- 유효한 입력(1~3)만 받기
- 무효한 입력(4 이상) 시 재입력 요청
- 입력한 직업을 저장하여 이후 활용

<br>
<br>
<br>

## 🔹 **기본 구조**

---

| 기능 | 설명 |
| --- | --- |
| **직업 선택 화면 출력** | 직업 목록 출력 |
| **입력 처리** | 사용자가 선택한 직업 번호 입력 |
| **입력 검증** | 1~3이면 선택 완료, 그 외 입력 시 재요청 |
| **반환값 활용** | 선택한 직업을 저장하여 이후 사용 |

<br>
<br>
<br>

## 🔹 **기본 직업 선택 코드**

---

```csharp
using System;

namespace TextRPG
{
    public static class Program
    {
        enum ClassType
        {
            None = 0,
            Knight = 1,
            Archer = 2,
            Mage = 3,
        }

        static void Main(string[] args)
        {
            ClassType choice = ClassType.None;

            while (choice == ClassType.None) // 유효한 선택을 받을 때까지 반복
            {
                Console.WriteLine("직업을 선택하세요!");
                Console.WriteLine("[1] 기사");
                Console.WriteLine("[2] 궁수");
                Console.WriteLine("[3] 법사");

                string input = Console.ReadLine();
                switch (input)
                {
                    case "1":
                        choice = ClassType.Knight;
                        break;
                    case "2":
                        choice = ClassType.Archer;
                        break;
                    case "3":
                        choice = ClassType.Mage;
                        break;
                    default:
                        Console.WriteLine("잘못된 입력입니다. 다시 선택해주세요.");
                        break;
                }
            }

            Console.WriteLine($"당신은 {choice}을(를) 선택했습니다.");
        }
    }
}
```

✅ **기능 요약**

1. while 문을 이용한 무한 루프
    - `choice == ClassType.None`이면 계속 반복 (유효한 선택을 받을 때까지)
2. `Console.ReadLine()`으로 입력 받기
3. switch 문으로 입력 검증
    - `"1"`, `"2"`, `"3"`만 허용
    - 잘못된 입력 시 오류 메시지 출력 후 재입력 요청
4. 정상 입력 시 루프 탈출 후 직업 출력

## 🔹 **개선: `ChooseClass()` 함수로 코드 분리**

---

- 직업 선택을 별도 함수로 분리하여 가독성 향상
- 유효한 입력을 받을 때까지 반복하는 구조 유지

```csharp
using System;

namespace TextRPG
{
    public static class Program
    {
        enum ClassType
        {
            None = 0,
            Knight = 1,
            Archer = 2,
            Mage = 3,
        }

        // 🔹 직업 선택 함수
        static ClassType ChooseClass()
        {
            while (true)
            {
                Console.WriteLine("직업을 선택하세요!");
                Console.WriteLine("[1] 기사");
                Console.WriteLine("[2] 궁수");
                Console.WriteLine("[3] 법사");

                string input = Console.ReadLine();
                switch (input)
                {
                    case "1": return ClassType.Knight;
                    case "2": return ClassType.Archer;
                    case "3": return ClassType.Mage;
                    default:
                        Console.WriteLine("잘못된 입력입니다. 다시 선택해주세요.");
                        break;
                }
            }
        }

        static void Main(string[] args)
        {
            ClassType choice = ChooseClass(); // 🔹 선택한 직업 저장
            Console.WriteLine($"당신은 {choice}을(를) 선택했습니다.");
        }
    }
}
```

✅ **개선된 점**

1. 코드 재사용 가능
    - `ChooseClass()`를 여러 곳에서 호출 가능
2. Main 함수 가독성 향상
    - `ClassType choice = ChooseClass();` 한 줄만으로 직업 선택 가능
3. 유지보수 용이
    - 직업 추가/변경 시 `ChooseClass()`만 수정하면 됨

<br>
<br>
<br>

## 🔹 번외) **추가 개선: `TryParse()`를 활용한 입력 검증**

---

- 문자열을 **정수(int)** 로 변환하여 처리 (예외 발생 방지)

```csharp
static ClassType ChooseClass()
{
    while (true)
    {
        Console.WriteLine("직업을 선택하세요!");
        Console.WriteLine("[1] 기사");
        Console.WriteLine("[2] 궁수");
        Console.WriteLine("[3] 법사");

        string input = Console.ReadLine();

        // 🔹 문자열 → 숫자 변환 (유효한 숫자인지 검사)
        if (int.TryParse(input, out int classNumber))
        {
            if (classNumber >= 1 && classNumber <= 3)
                return (ClassType)classNumber; // 🔹 정수 → Enum 변환
        }

        Console.WriteLine("잘못된 입력입니다. 다시 선택해주세요.");
    }
}
```

✅ **추가된 장점**

- `int.TryParse()` → **입력값이 숫자인지 먼저 확인** (예외 발생 방지)
- 정수 값이 **1~3 범위 내에 있는지 검사**
- `return (ClassType)classNumber;` → 정수를 **직업 타입(Enum)** 으로 변환

<br>
<br>
<br>

## 🔹 **개선된 최종 코드**

---

```csharp
using System;

namespace TextRPG
{
    public static class Program
    {
        enum ClassType
        {
            None = 0,
            Knight = 1,
            Archer = 2,
            Mage = 3,
        }

        static ClassType ChooseClass()
        {
            while (true)
            {
                Console.WriteLine("직업을 선택하세요!");
                Console.WriteLine("[1] 기사");
                Console.WriteLine("[2] 궁수");
                Console.WriteLine("[3] 법사");

                string input = Console.ReadLine();

                if (int.TryParse(input, out int classNumber))
                {
                    if (classNumber >= 1 && classNumber <= 3)
                        return (ClassType)classNumber;
                }

                Console.WriteLine("잘못된 입력입니다. 다시 선택해주세요.");
            }
        }

        static void Main(string[] args)
        {
            ClassType choice = ChooseClass();
            Console.WriteLine($"당신은 {choice}을(를) 선택했습니다.");
        }
    }
}
```

✅ **최적화된 기능**

1. 입력값을 정수로 변환 (`TryParse()`) → 숫자가 아닌 입력 방지
2. 유효한 범위(1~3) 검증 → `if (classNumber >= 1 && classNumber <= 3)`
3. 함수 분리로 가독성 향상 → `ChooseClass()` 활용

<br>
<br>
<br>

## 📌 **정리**

---

| 기능 | 설명 |
| --- | --- |
| **직업 선택 시스템 구현** | 1~3 입력 시 직업 선택 완료 |
| **잘못된 입력 처리** | 범위 밖의 값 입력 시 재요청 |
| **함수 분리 (`ChooseClass()`)** | 직업 선택 기능을 함수화 |
| **`TryParse()` 활용** | 숫자 변환 실패 방지 |
| **Enum 활용 (`ClassType`)** | 코드 가독성 향상 및 유지보수 편의성 증가 |