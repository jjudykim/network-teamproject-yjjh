## 메소드 오버로딩

### 🔹 **메소드 오버로딩 (Method Overloading)**

---

- 같은 이름을 가진 여러 개의 메소드를 정의할 수 있는 기능.
- 매개변수의 개수 또는 자료형이 다를 경우 유효함.
- 반환형만 다른 경우 오버로딩 불가.

<br>
<br>
<br>

### 🔹 **메소드 오버로딩 기본 문법**

---

```csharp
static int Add(int a, int b)
{
    Console.WriteLine("Add(int, int) 호출");
    return a + b;
}

static int Add(int a, int b, int c)
{
    Console.WriteLine("Add(int, int, int) 호출");
    return a + b + c;
}

static float Add(float a, float b)
{
    Console.WriteLine("Add(float, float) 호출");
    return a + b;
}
```

✅ **호출 예시**

```csharp
int sum1 = Add(2, 3);         // Add(int, int) 호출
int sum2 = Add(1, 2, 3);      // Add(int, int, int) 호출
float sum3 = Add(2.0f, 3.0f); // Add(float, float) 호출
```

<br>
<br>
<br>

### 🔹 **오버로딩 시 주의할 점**

---

1️⃣ **반환형만 다를 경우 오버로딩 불가**

```csharp
static int Add(int a, int b) { return a + b; }
static float Add(int a, int b) { return a + b; } // ❌ 오류 발생
```

→ 반환형은 오버로딩을 결정하는 요소가 아님.

2️⃣ **매개변수의 순서만 변경해도 오버로딩 불가**

```csharp
static int Add(int a, float b) { return a + (int)b; }
static int Add(float b, int a) { return (int)b + a; } // ❌ 오류 발생
```

→ 매개변수의 자료형과 개수가 동일하면 오버로딩이 불가능.

<br>
<br>
<br>

## 선택적 매개변수

### 🔹 **선택적 매개변수 (Optional Parameters)**

---

- 기본값을 설정하여 인자를 생략할 수 있는 기능.
- 매개변수 기본값을 제공하면 생략 가능.

```csharp
static int Add(int a, int b, int c = 0)
{
    Console.WriteLine("Add(int, int, int) 호출");
    return a + b + c;
}
```

✅ **호출 예시**

```csharp
int sum1 = Add(2, 3);    // c가 기본값 0으로 사용됨 → 5 반환
int sum2 = Add(2, 3, 4); // 명시적으로 c = 4를 전달 → 9 반환
```

<br>
<br>


### 🔹 **선택적 매개변수의 확장**

---

```csharp
static int Add(int a, int b, int c = 0, float d = 1.0f, double e = 3.0)
{
    Console.WriteLine("Add 호출");
    return a + b + c;
}
```

✅ **호출 예시**

```csharp
int sum1 = Add(2, 3);           // 기본값 사용
int sum2 = Add(1, 2, d: 2.0f);  // d만 명시적으로 변경
```

✅ **주의점**

- 기본값이 설정된 매개변수는 **반드시 뒤쪽에 배치**해야 함.

```csharp
static int Add(int a = 1, int b) { return a + b; } // ❌ 오류 발생
```

<br>
<br>

### 📌 **정리**

---

- **오버로딩**: 같은 이름의 메소드를 매개변수 개수/자료형을 달리하여 정의 가능.
- 반환형만 다르면 오버로딩 불가능.
- **선택적 매개변수**: 기본값을 제공하여 일부 인자를 생략할 수 있음.
- 명명된 매개변수를 활용하면 특정 인자만 지정 가능.