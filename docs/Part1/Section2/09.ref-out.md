## ref

### 🔹 **`ref`와 반환값의 차이**

---

- **반환값 (Return) 사용**
    - 기존 값 변경 없이 새로운 값을 반환.
    - 유연성이 높고 범용적으로 사용 가능.
- **`ref` 사용**
    - 원본 데이터를 직접 수정해야 할 때 사용.
    - 값이 아니라 **메모리 주소(참조)**를 전달하여 직접 변경.

<br>
<br>

### 🔹 **반환값 (Return) 방식 예제**

---

```csharp
static int AddOne(int number)
{
    return number + 1;
}

static void Main()
{
    int a = 0;
    a = AddOne(a);         // 반환값을 변수에 저장
    Console.WriteLine(a);  // 출력: 1
}
```

✅ **특징**

- `AddOne()`은 기존 값을 수정하지 않음.
- 호출한 쪽에서 결과를 받을지 여부를 선택할 수 있음.
- **원본 값을 유지하면서 다양한 방식으로 활용 가능.**

<br>
<br>

### 🔹 **`ref` 방식 예제**

---

```csharp
static void AddOne(ref int number)
{
    number = number + 1;
}

static void Main()
{
    int a = 0;
    AddOne(ref a);         // 원본이 직접 변경됨
    Console.WriteLine(a);  // 출력: 1
}
```

✅ **특징**

- `ref` 키워드를 사용하면 원본 값을 직접 변경 가능.
- 반환값 없이도 원본을 수정할 수 있음.
- 다른 곳에서 원본 값을 유지할 필요가 없을 때 적절함.

<br>
<br>

### 🔹 **두 방식의 비교**

---

| 구분 | 반환값 사용 | `ref` 사용 |
| --- | --- | --- |
| 원본 값 유지 | ✅ 가능 | ❌ 변경됨 |
| 유연성 | ✅ 높음 | ❌ 제한적 |
| 사용 예시 | 덧셈, 곱셈 등 계산 후 새로운 값 반환 | 원본 값 수정이 필요한 경우 (ex. Swap) |
| 다중 반환 | ❌ 기본적으로 1개만 반환 가능 | ✅ 여러 값 수정 가능 |

✅ **결론**

- 원본 값이 필요하면 **반환값 방식이 더 유용**.
- 원본을 직접 변경해야 하면 **`ref` 방식이 필요**.

<br>
<br>

### 🔹 **`ref`가 필요한 경우: Swap 함수**

---

```csharp
static void Swap(ref int a, ref int b)
{
    int temp = a;
    a = b;
    b = temp;
}

static void Main()
{
    int num1 = 1;
    int num2 = 2;
    Swap(ref num1, ref num2);
    Console.WriteLine($"{num1}, {num2}");  // 출력: 2, 1
}
```

✅ **특징**

- 두 변수 값을 직접 교환해야 하므로 **`ref` 필수**.
- 반환값 방식으로는 원본 교환 불가.

<br>
<br>

### 📌 **정리**

---

- **반환값 (`return`) 방식이 기본적으로 더 유연하고 선호됨**.
- **`ref`는 원본을 직접 수정해야 하는 경우 사용**.
- 대표적인 `ref` 활용 예시: **Swap 함수**
- `ref` 없이 단순히 값을 조작할 경우 **반환값 방식이 더 좋음**.

<br>
<br>
<br>

## out

<br>
<br>


### **`out` 키워드란?**

---

- 함수가 여러 개의 값을 반환할 때 사용.
- `ref`와 유사하지만 반드시 값을 할당해야 함.
- 함수 내부에서 값을 무조건 초기화해야 하는 규칙이 있음.

<br>
<br>


### 🔹 **`out`과 `ref` 비교**

---

| 구분 | `ref` (참조 전달) | `out` (출력 매개변수) |
| --- | --- | --- |
| 원본 변수 필요 | ✅ 필요 (초기화되어 있어야 함) | ❌ 불필요 (함수 내부에서 할당 필수) |
| 함수 내부에서 값 할당 | ❌ 선택적 | ✅ 필수 |
| 여러 개의 값 반환 가능 | ✅ 가능 | ✅ 가능 |
| 주요 사용 사례 | 원본 값 변경이 필요할 때 | 여러 개의 값을 반환할 때 |


<br>
<br>


### 🔹 **`out`을 활용한 다중 반환 예제**

---

```csharp
static void Divide(int a, int b, out int quotient, out int remainder)
{
    quotient = a / b;  // 몫 계산
    remainder = a % b; // 나머지 계산
}

static void Main()
{
    int num1 = 10, num2 = 3;
    int result1, result2;

    Divide(num1, num2, out result1, out result2);

    Console.WriteLine($"몫: {result1}, 나머지: {result2}");
}
```

✅ **출력 결과**

```
몫: 3, 나머지: 1
```

✅ **특징**

- `result1`과 `result2`는 함수 호출 전에는 초기화할 필요 없음.
- 함수 내부에서 반드시 값이 할당되어야 함.
- `out` 키워드를 사용하여 다중 값을 반환.

### 🔹 **`ref` vs `out` 코드 비교**

---

✅ **`ref` 사용**

```csharp
csharp
복사편집
static void ModifyValue(ref int number)
{
    number += 10;  // 기존 값 변경
}

static void Main()
{
    int value = 5;
    ModifyValue(ref value);
    Console.WriteLine(value);  // 출력: 15
}

```

✅ **특징**

- `ref`를 사용하면 원본 값이 변경됨.
- `value`는 초기화된 상태에서 함수에 전달.

✅ **`out` 사용**

```csharp
static void GenerateNumbers(out int num1, out int num2)
{
    num1 = 10;  // 값 할당 필수
    num2 = 20;
}

static void Main()
{
    int a, b;
    GenerateNumbers(out a, out b);
    Console.WriteLine($"{a}, {b}");  // 출력: 10, 20
}
```

✅ **특징**

- `out`을 사용하면 **초기화 없이 전달 가능**.
- 함수 내부에서 **반드시 값이 할당되어야 함**.

<br>
<br>


### 🔹 **언제 `out`을 사용할까?**

---

1️⃣ **함수에서 여러 개의 값을 반환해야 할 때**

```csharp
static void GetPlayerStats(out int hp, out int mp)
{
    hp = 100;
    mp = 50;
}
```

2️⃣ **값을 반환하지만, 기존 변수를 변경할 필요가 없을 때**

```csharp
static bool TryParseInt(string input, out int number)
{
    return int.TryParse(input, out number);
}
```

✅ **대표적인 예시: `int.TryParse()`**

```csharp
string userInput = "123";
if (int.TryParse(userInput, out int result))
{
    Console.WriteLine($"변환 성공: {result}");  // 출력: 변환 성공: 123
}
```

- `TryParse()`는 문자열을 숫자로 변환할 수 있는지 검사 후, 성공하면 숫자를 반환.

<br>
<br>

### 📌 **정리**

---

- `out`은 함수가 여러 개의 값을 반환할 때 유용.
- `out`을 사용하면 **초기화 없이 값 전달 가능**, 단 **함수 내부에서 값 할당 필수**.
- `ref`는 기존 변수를 참조하여 **원본 값을 직접 변경**.
- 일반적으로 다중 반환이 필요하면 `out`, 기존 값 변경이 필요하면 `ref`를 사용.