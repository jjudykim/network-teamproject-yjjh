### 🔹 클래스 분리의 필요성

---

- 기존에는 모든 클래스를 하나의 `.cs` 파일에 몰아넣었음
- 파일이 커지고 가독성이 떨어지는 문제 발생
- 객체지향적으로 설계할 때는 클래스 단위로 파일을 분리하는 것이 유지보수에 유리

  > 예: Player.cs, Monster.cs 등을 각각 따로 분리
>
- C#은 C++과 달리 `#include` 없이도 같은 namespace 내에 있으면 클래스들을 자유롭게 참조 가능

<br>
<br>

### 🔹 프로젝트 구조 세팅

---

1. 솔루션 탐색기에서 프로젝트 우클릭 → [추가] → [새 항목] → [클래스]
2. 예: `Player.cs` 생성 → 클래스 이름 수정
3. 복사해서 새 클래스 만들 경우: 반드시 이름과 내부 내용도 수정할 것

<br>
<br>

### 🔹 기본 직업 구분용 enum 정의

---

```csharp
enum PlayerType
{
	None = 0,
	Knight = 1,
	Archer = 2,
	Mage = 3,
}
```

- 직업 구분을 위한 열거형
- 플레이어 객체 내부에서 본인의 타입 정보를 가지도록 설계

<br>
<br>

### 🔹 Player 클래스 설계

---

```csharp
class Player
{
	protected PlayerType type;
	protected int hp;
	protected int attack;

	protected Player(PlayerType type)
	{
		this.type = type;
	}

	public void SetInfo(int hp, int attack)
	{
		this.hp = hp;
		this.attack = attack;
	}

	public int GetHp() { return hp; }
	public int GetAttack() { return attack; }

	public bool IsDead() { return hp <= 0; }

	public void OnDamaged(int damage)
	{
		hp -= damage;
		if (hp < 0)
			hp = 0;
	}
}
```

- `type`, `hp`, `attack`은 외부 직접 접근을 막고, 자식 클래스에서 접근할 수 있도록 `protected`로 설정
- 생성자에서 `type`을 반드시 설정하도록 유도
- `SetInfo()` 함수로 초기 체력/공격력을 세팅
- 체력 반환, 공격력 반환, 데미지 처리 기능 포함

<br>
<br>

### 🔹 Player 클래스를 직접 인스턴스화하지 못하게 설계

---

- 생성자를 `protected`로 설정 → 외부에서 `new Player()` 불가
- 반드시 자식 클래스(Knight 등)를 통해서만 생성 가능
- 의도하지 않은 기본 객체 생성을 막는 은닉성(Encapsulation)의 실천

<br>
<br>

### 🔹 자식 클래스 정의 (Knight, Archer, Mage)

---

```csharp
class Knight : Player
{
	public Knight() : base(PlayerType.Knight)
	{
		SetInfo(100, 10);
	}
}

class Archer : Player
{
	public Archer() : base(PlayerType.Archer)
	{
		SetInfo(75, 12);
	}
}

class Mage : Player
{
	public Mage() : base(PlayerType.Mage)
	{
		SetInfo(50, 15);
	}
}
```

- 각 클래스는 부모 클래스의 생성자를 호출하며 고유한 `PlayerType` 전달
- 기본 체력 및 공격력은 하드코딩 (향후 데이터 파일로 분리 가능)

<br>
<br>

### 🔹 네이밍 컨벤션 팁

---

- 멤버 변수에 접두어 `_` 또는 `m_`을 붙이는 관례 존재
    - ex: `_hp`, `m_attack`
    - 해당 네이밍 컨벤션을 통해 `this.` 생략 가능하고 가독성 향상됨
- 팀이나 프로젝트에서 사용하는 컨벤션에 따라 결정

<br>
<br>

### ✅ 결론

---

- 이번 설계에서는 OOP의 은닉성과 생성자 설계를 적극 활용
- 직업별 클래스를 분리하고, 공통 로직은 `Player` 부모 클래스에 통합
- `Player`를 직접 만들지 못하도록 하여 객체 생성의 안정성을 확보

<br>
<br>

### 📌 **최종 코드**

---

```csharp
using System;
using System.Collections.Generic;
using System.Text;

namespace CSharp
{
	enum PlayerType
	{
		None = 0,
		Knight = 1,
		Archer = 2,
		Mage = 3,
	}
	
	class Player
	{
		protected PlayerType type;
		protected int hp;
		protected int attack;
		
		public Player(PlayerType type)
		{
			this.type = type;
		}
		
		public void SetInfo(int hp, int attack)
		{
			this.hp = hp;
			this.attack = attack;
		}
		
		public int GetHp() { return hp; }
		public int GetAttack() { return attack; }
		public bool IsDead() { return hp <= 0; }
		public void OnDamaged(int damage)
		{
			hp -= damage;
			if (hp < 0)
				hp = 0;
		}
	}
	
	class Knight : Player
	{
		public Knight() : base(PlayerType.Knight)
		{
			SetInfo(100, 10)
		}
	}
	
	class Archer: Player
	{
		public Archer() : base(PlayerType.Archer)
		{
			SetInfo(75, 12);
		}
	}
	
	class Mage : Player
	{
		public Mage() : base(PlayerType.Mage)
		{
			SetInfo(50, 15);
		}
	}
}
```