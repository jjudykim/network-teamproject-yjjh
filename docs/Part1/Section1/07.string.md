### string

---

문자열을 표현하는 데 사용

문자열 → 문자의 배열, 나열

```csharp
string name;
name = "Rookiss"
```

- 유니코드 기반이기 때문에 영어 뿐만 아니라 한국어, 중국어 등 사용 가능
- `“”` 를 사용해 원하는 문자열을 감싸 저장할 수 있음
- 내부적으로는 문자 하나하나를 하나의 정수로 저장하고 있음 → ASCII 코드표 참조

유사한 `char`라는 타입 존재 → 문자를 딱 한 개만 저장