### float

---

부동소수점, 혹은 실수

- 정수형과 다르게 완전히 정확하게 표현되는 타입이 X

  근사치로 표현이 된다

  → 메모리 공간이 유한하기 때문에, 무한대의 데이터를 다 표현할 수는 없으니까!

- `f`, `F` 접미사를 붙여야 float 타입으로 인식

유사하게 `double` 타입이 존재

→ 더 정밀도가 높고, 메모리 점유 공간이 더 큼(8byte)

`F`, `f` 접미사를 사용하지 않으면 소숫점이 붙은 숫자를 기본적으로 double 타입으로 인식