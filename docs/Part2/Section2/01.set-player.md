# 스크립트

### 🔹 **스크립트 템플릿 주석 제거**

---

- 새 스크립트 생성 시 자동으로 붙는 주석 제거 가능
- 경로 예시:

  `C:\Program Files\Unity\Hub\Editor\[버전]\Editor\Data\Resources\ScriptTemplates`

- `81-C# Script-NewBehaviourScript.cs.txt` 파일 수정
    - 상단 주석 삭제 후 저장
    - 권한 문제 시 바탕화면으로 복사 → 수정 → 원래 경로로 덮어쓰기

<br>
<br>

### 🔹 **유니티짱(Unity Chan) 모델 다운로드 및 구성요소**

---

1. **Asset Store 접근**
    - 유니티짱(“Unity Chan”) 검색 → 다운로드 → Import
    - 애니메이션, 모델, 머티리얼, 오디오 중 필요한 것만 선택하여 임포트
2. **유니티짱 구조**
    - 모델: 캐릭터 본체 (FBX 파일)
    - 머티리얼: 재질, 의상 및 색상 정보
    - 애니메이션: 동작 모션들 (Jump, Run 등)
    - 오디오: 유니티짱 보이스 포함
    - 각 부위는 Empty 오브젝트 + 컴포넌트 조합으로 구성
3. **참고 기능**
    - `Find References In Scene`: 머티리얼 사용처 추적
    - 씬 뷰 모드: `Shaded`, `Wireframe` 등 변경 가능
    - 실제 3D 모델은 모두 삼각형(트라이앵글)로 구성됨

<br>
<br>

### 🔹 **프리팹(Prefab) 개념 간략 소개**

---

- 여러 구성요소(컴포넌트)를 조합하여 만든 **재사용 가능한 오브젝트**
- 유니티짱도 프리팹으로 구성되어 있음
- 나중에 더 자세히 학습 예정

<br>
<br>


### 🔹 **플레이어 조작 스크립트 기본 구현**

---

Player Controller Script 생성

```csharp
public class PlayerController : MonoBehaviour
{
    void Start() { }

    // GameObject (Player)
    // ㄴ Transform
    // ㄴ PlayerController (Component)

    void Update()
    {
        if (Input.GetKey(KeyCode.W))
            transform.position += new Vector3(0f, 0f, 1f);
        if (Input.GetKey(KeyCode.S))
            transform.position -= new Vector3(0f, 0f, 1f);
        if (Input.GetKey(KeyCode.A))
            transform.position -= new Vector3(1f, 0f, 0f);
        if (Input.GetKey(KeyCode.D))
            transform.position += new Vector3(1f, 0f, 0f);
    }
}
```

1. **중요 개념 요약**
- `transform`: 현재 스크립트가 붙어 있는 게임 오브젝트의 위치/회전/스케일을 제어
- `Input.GetKey(KeyCode.키)`: 해당 키가 눌렸는지 매 프레임 체크
- `position += Vector3`: 위치값을 직접 수정해서 이동 처리

1. **실행 시 주의사항**
- 실행 후 **Game 뷰 클릭** 필요 (포커스 설정)
- 너무 빠르게 이동되는 현상은 `Time.deltaTime` 적용으로 후속 보완 예정

<br>
<br>

### ✅ 결론

---

- 유니티짱 캐릭터를 **모델 + 머티리얼 + 애니메이션** 구성으로 이해
- 사용자 입력을 받아 **Transform.position** 조작으로 기본 이동 구현
- 실습을 통해 모델 배치 → 스크립트 연결 → 입력 처리 흐름을 학습