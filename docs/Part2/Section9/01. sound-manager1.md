# 스크립트

### 🔹 유니티의 사운드 구성 요소 (3대 요소)

---

| ⭐ Unity 컴포넌트 | 현실 예시 | 역할 |
| --- | --- | --- |
| `AudioSource` | 🎵 MP3 플레이어 | 소리를 발생시키는 재생기 |
| `AudioClip` | 💽 음원(mp3 파일) | 실제 재생할 사운드 데이터 |
| `AudioListener` | 👂 귀 | 소리를 듣는 대상, 보통 카메라에 부착됨 |
- `AudioListener`: 씬에 **1개만 존재해야 하며**, 기본적으로 카메라에 자동 부착되어 있음.
- `AudioSource`: 재생을 담당하는 컴포넌트. GameObject에 추가하여 사용.
- `AudioClip`: mp3, wav 등의 사운드 파일을 유니티에서 사용하는 객체.

<br>
<br>

### 🔹SoundManager 클래스 설계 시작!

---

- `SoundManager` 클래스 생성 → `MonoBehaviour`, `Start`, `Update` 제거 후 빈 클래스 상태로 시작함

    ```csharp
    public class SoundManager
    {
    
    }
    ```


- `Managers` 클래스에 `_sound` 필드, `Sound` 프로퍼티 추가

    ```csharp
    public class Managers : MonoBehaviour
    {
    	// ...
    	SoundManager _sound = new SoundManager();
    	
    	public static SoundManager Sound { get { return Instance._sound; } }
    	
    	// ...
    }
    ```

<br>
<br>

### 🔹 사운드 테스트 실습 – AudioSource 활용

---

1. **Cube Object에 `AudioSource` 추가**
2. `AudioClip` 설정 (유니티짱 폴더에 있는 소리 사용)
3. `Play On Awake`, `Loop`, `Volume`, `Pitch` 등 기능 실험

    | `Play On Awake` | 씬 시작 시 자동 재생 |
    | --- | --- |
    | `Loop` | 반복 재생 여부 |
    | `Volume` | 음량 |
    | `Pitch` | 재생 속도 |
    | `Mute` | 음소거 |

<br>
<br>

### 🔹 스크립트로 사운드 제어 테스트

---

```csharp
public class TestSound : MonoBehaviour
{
	public AudioClip audioClip;

	private void OnTriggerEnter(Collider other)
	{
		AudioSource audio = GetComponent<AudioSource>();
		audio.PlayOneShot(audioClip);
	}
}
```

- `TestSound.cs` 생성 → `Cube2`에 부착
- `OnTriggerEnter()` 이벤트에서 사운드 실행
- `GetComponent<AudioSource>()`로 오디오 소스 가져옴
- `PlayOneShot(AudioClip)`으로 사운드 재생
    - 동시에 여러 사운드도 가능 (겹쳐서 재생됨) → **기존 재생과 상관없이 즉시 재생**됨
- 기존 `Play()`는 현재 클립만 재생하며 새 클립 재생 시 덮어쓰기됨

<br>

**PlayOneShot 다중 재생 실험**

```csharp
public class TestSound : MonoBehaviour
{
	public AudioClip audioClip1;
	public AudioClip audioClip2;

	private void OnTriggerEnter(Collider other)
	{
		AudioSource audio = GetComponent<AudioSource>();
		audio.PlayOneShot(audioClip1);
		audio.PlayOneShot(audioClip2);
	}
}
```

- 두 사운드가 동시에 재생됨 → `AudioSource`는 **여러 PlayOneShot을 동시에 실행 가능**
- `AudioSource.PlayOneShot(AudioClip)`는
    - **재생 중인 사운드를 덮지 않고** 새로운 사운드를 별도로 재생
    - 여러 사운드의 동시 재생 가능

<br>
<br>

### 🔹 사운드 도중 GameObject 삭제 시

---

```csharp
GameObject.Destroy(gameObject, 0.25f);
```

- 사운드가 완전히 재생되기 전에 GameObject가 삭제되면 **소리도 끊김**

**💡 해결 방법**

→  `AudioClip.length`를 이용해 가장 긴 재생 시간 기준으로 삭제 예약

```csharp
float lifeTime = Mathf.Max(audioClip1.length, audioClip2.length);
GameObject.Destroy(gameObject, lifeTime);
```

- 단, 객체가 사운드 외에도 애니메이션 등 다양한 기능을 가질 경우 Sound 기준으로 파괴 타이밍을 잡는 건 비효율적이고 **의미 불명**

<br>
<br>

### ✅ **결론 및 정리**

---

이건 기억하자!

| ⭐ Unity 컴포넌트 | 역할 |
| --- | --- |
| `AudioSource` | 소리를 발생시키는 재생기 |
| `AudioClip` | 실제 재생할 사운드 데이터 |
| `AudioListener` | 소리를 듣는 대상, 보통 카메라에 부착됨, 씬 당 하나만 존재! |

**개선 방향**

- 사운드를 실행할 객체가 곧 사라질 수 있다면 → `AudioSource`를 매니저가 관리하도록 구조 변경 필요
- 즉, 큐브 등 게임 오브젝트가 아닌 **SoundManager**에서 직접 `AudioSource`를 가지고 있어야 안정적

→ 따라서 **SoundManager가 AudioSource를 들고 직접 재생을 관리하는 방식**으로 개선할 예정