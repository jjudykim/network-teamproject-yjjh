### 🔹 **Quarter View 카메라란?**

---

- 쿼터뷰는 RPG 게임 등에서 흔히 사용되는 시점으로, **플레이어를 위에서 비스듬히 내려다보는 카메라 시점**임
- 카메라는 플레이어를 항상 따라가며 일정한 각도와 거리를 유지함
- 카메라는 회전하지 않고, 고정된 각도로 플레이어를 내려다보며 움직이기만 함

<br>
<br>

### 🔹 **기본 구조**

---

- 카메라에 별도 스크립트 `CameraController`를 부착
- 카메라는 플레이어를 따라가되, **회전은 고정** + **위치만 추적**
- 카메라 위치는 `플레이어 위치 + 오프셋 벡터(delta)`로 계산
- 카메라는 항상 `transform.LookAt()`으로 플레이어를 응시

**구현 순서**

1. `CameraController` 스크립트 생성 후 Main Camera에 부착
2. 플레이어 위치 오프셋을 Vector3로 저장 (`_delta`)
3. 플레이어 GameObject 참조를 받을 수 있도록 `GameObject _player` 필드 정의
4. 유니티 에디터에서 MainCamera에 delta와 player 연결
5. `LateUpdate()`에서 카메라 위치/회전 갱신

**CameraMode 정의 (확장 대비)**

```csharp
public class Define
{
    public enum CameraMode
    {
        QuarterView,
    }
}
```

- 카메라 모드를 명시적으로 구분 가능
- 향후 1인칭, 탑다운 등 다양한 모드를 추가할 수 있음

<br>
<br>

### 🔹 **CameraController 구현**

---

```csharp
using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class CameraController : MonoBehaviour
{
    [SerializeField]
    Define.CameraMode _mode = Define.CameraMode.QuarterView;

    [SerializeField]
    Vector3 _delta = new Vector3(0, 6, -5);         // 플레이어 기준 거리

    [SerializeField]
    GameObject _player;

    void LateUpdate()
    {
        if (_mode == Define.CameraMode.QuarterView)
        {
            transform.position = _player.transform.position + _delta;
            transform.LookAt(_player.transform);
        }
    }

    public void SetQuarterView(Vector3 delta)
    {
        _mode = Define.CameraMode.QuarterView;
        _delta = delta;
    }
}
```

**왜 LateUpdate()를 사용했을까?**

- `Update()`는 게임 로직이 동시에 실행되어 **플레이어 이동보다 카메라 이동이 먼저 되거나 늦을 수 있음**
- 그로 인해 카메라가 미세하게 떨리는 현상 발생 (프레임 간 위치 불일치)
- `LateUpdate()`는 모든 Update 로직이 끝난 후 실행되므로, **플레이어 위치 갱신 이후에 카메라 위치 갱신 가능**
- 카메라 떨림 현상을 완전히 해결할 수 있음

**LookAt() 함수란?**

- `transform.LookAt(target)`

  → 해당 transform이 항상 `target`을 바라보도록 회전값(Rotation)을 자동으로 설정함

- 회전 수식이나 Quaternion 계산 없이도 시선 고정이 가능

<br>
<br>

### 💭 번외

---

**변수 처리 팁**

- `_player`와 `_delta`는 `[SerializeField] private`로 선언하여 외부 접근은 막고 인스펙터 노출은 유지
- `_mode`는 enum으로 상태 표현을 명확하게 구분

<br>
<br>

### ✅ **결론 및 정리**

---

- 쿼터뷰 카메라는 RPG나 액션 게임에서 자주 사용되는 시점이며, **카메라가 플레이어를 일정 거리에서 응시하며 따라가는 구조**로 설계됨
- Unity에서는 `transform.position + offset`으로 간단히 구현 가능하며, `LookAt()`을 활용해 시선을 고정할 수 있음
- **Update가 아닌 LateUpdate를 사용**함으로써 플레이어 이동 이후에 카메라가 반응하게 되어 **카메라 떨림 문제를 해결**할 수 있음
- enum을 이용한 모드 구분 및 delta 벡터를 통한 거리 설정 방식은 **확장성**과 **가독성**을 동시에 확보함
- 카메라는 한 번 세팅되면 수정 빈도가 낮지만, 게임 전반적인 몰입감과 시야 확보에 큰 영향을 주므로 **처음 설계가 매우 중요함**

### 📌 최종 코드

---

```csharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Define
{
    public enum CameraMode
    {
        QuarterView,
    }
}
```

```csharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{
    [SerializeField]
    Define.CameraMode _mode = Define.CameraMode.QuarterView;

    [SerializeField]
    Vector3 _delta = new Vector3(0.0f, 6.0f, -5.0f);

    [SerializeField]
    GameObject _player = null;

    void Start()
    {
        
    }

    void LateUpdate()
    {
        if (_mode == Define.CameraMode.QuarterView)
        {
            transform.position = _player.transform.position + _delta;
            transform.LookAt(_player.transform);
        }
    }

    public void SetQuarterView(Vector3 delta)
    {
        _mode = Define.CameraMode.QuarterView;
        _delta = delta;
    }
}
```